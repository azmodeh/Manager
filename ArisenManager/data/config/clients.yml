# Telegram Clients Configuration
bot_types:
  userbot: "UserBot"
  apibot: "API Bot"

config_keys:
  sudo_id: "sudo_id"
  sudo_fallback: "sudo"
  api_id: "api_id"
  api_hash: "api_hash"
  bot_token: "bot_token"